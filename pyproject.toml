[project]
name = "koralldata"
version = "0.1.0"
description = "Queries Koralldata because the web interface is terrible"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.3",
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
]
